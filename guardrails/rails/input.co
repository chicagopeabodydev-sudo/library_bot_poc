import core

flow input rails $input_text
  library input blocked terms

flow library input blocked terms
  $is_allowed = await CheckLibraryInputAction()

  if not $is_allowed
    bot say "Sorry, I can only help with safe questions about the indexed library website."
    abort
