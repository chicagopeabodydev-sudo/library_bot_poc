import core

flow output rails $output_text
  library output blocked terms

flow library output blocked terms
  $is_allowed = await CheckLibraryOutputAction()

  if not $is_allowed
    bot say "Sorry, I can't provide that response."
    abort
